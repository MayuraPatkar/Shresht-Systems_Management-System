/**
 * Calculations Module Types
 */

interface ConversionMap {
    [key: string]: number;
}

interface UnitNames {
    [key: string]: string;
}

interface KnownGeometry {
    angle: boolean;
    run: boolean;
    rise: boolean;
    hyp: boolean;
}
